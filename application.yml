security:
  basic:
    enabled: false

management:
  security:
    enabled: false

logging:
  level:
    io:
      pivotal: DEBUG
      
spring: # <---NEW SECTION
  cloud:
    services:
      registrationMethod: route
      
fortune-service:
  ribbon:
    MaxAutoRetriesNextServer: 1
    
logging:
  level:
    org.springframework.retry: DEBUG